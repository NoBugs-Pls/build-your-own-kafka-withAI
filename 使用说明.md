# SimpleKafka 使用说明

一个用 Java 实现的类 Kafka 消息集群：ZooKeeper 元数据协调、Broker 分区持久化、
Controller 选举、Leader→Follower 复制、生产者发送与消费者拉取，并配有浏览器可视化控制台。

> 本项目的核心代码与 `build-your-own-kafka-main` 参考实现保持一致的协议与元数据约定。

---

## 1. 目录结构

```
pom.xml
src/main/java/com/simplekafka/
  broker/     BrokerInfo、Partition（segment log + index 持久化）、Protocol、
              SimpleKafkaBroker（网络、复制、controller 选举）、ZookeeperClient
  client/     SimpleKafkaClient、SimpleKafkaProducer、SimpleKafkaConsumer
  dashboard/  DashboardServer（HTTP 服务：进程管理 + 元数据 + 消息 + ZooKeeper 操控）
src/main/resources/
  controlroom/index.html   独立可视化控制台（推荐入口）
  dashboard/index.html     旧版观察页面
```

## 2. 端口规划

| 组件 | 端口 |
| --- | --- |
| 可视化控制台 | 8080 |
| ZooKeeper | 2181 |
| broker-1 / broker-2 / broker-3 | 9091 / 9092 / 9093 |

## 3. 构建与启动

```bash
cd /home/root/build-your-own-kafka
mvn -q -DskipTests package
java -cp target/simple-kafka-1.0-SNAPSHOT.jar \
  com.simplekafka.dashboard.DashboardServer 8080 2181
```

浏览器打开：

- 独立控制台（推荐）： http://localhost:8080/control （根路径 `/` 同样指向它）
- 旧观察页： http://localhost:8080/dashboard

点击右上角 **“启动集群”**，dashboard 会按你填写的「节点数 / 起始端口」自动拉起 ZooKeeper 与对应数量的 broker。
若端口已被旧进程占用，dashboard 会检测到并直接复用，不会报 BindException。

**节点数量不设上限，可自由增减：**

| 操作 | 位置 | 说明 |
| --- | --- | --- |
| 一次启动 N 个节点 | 右上角「节点数 / 起始端口」+「启动集群」 | 已有节点会被复用，缺少的会补齐 |
| **新增 Broker（可指定 ID / 端口）** | Broker 面板「Broker ID / 端口」+「新增 Broker」 | 想指定就填，留空自动分配（下一个 id 与下一个空闲端口） |
| 自动分配一个节点 | Broker 面板「自动分配一个」或右上角「＋ 增加节点」 | 等价于 id/端口都留空 |
| 移除一个节点 | Broker 行内「移除」 | 优雅停机，触发 shutdown hook 关闭 ZK 会话，临时节点立即注销 |
| 重启 / 强杀 | Broker 行内「重启」「强杀」 | 强杀用于故障演练（临时节点要等会话超时约 30 秒才消失） |
| 停止全部 | 右上角「停止全部」 | 停止所有 broker **与 ZooKeeper**；先发优雅信号，4 秒未退出自动升级为强杀，最后再兜底清扫，结束后按钮会提示停止数量 |

> 说明：停止全部会连同 ZooKeeper 一起停掉（集群完全下线）。只停部分节点请用行内「移除」。

**新增 Broker 的安全检查**（后端自动执行）：

1. 同 id 已有存活进程 → 拒绝，返回 `broker-X is already running`；
2. 目标端口已被占用 → 拒绝，返回 `port XXXX is already in use`；
3. ZooKeeper 中残留了同 id 的注册节点（例如该节点刚被强杀、会话还没过期）而进程已不存在
   → 自动清理该残留节点，新节点可立即注册，不会“隐身”。

> dashboard 会自动**接管**已在运行的 broker 进程（例如用命令行手动启动的节点，或重启 dashboard 之前启动的节点），
> 因此它们同样会显示在页面中并可被重启 / 强杀 / 移除。


## 4. 可视化控制台：四区同时操控

### 4.1 ZooKeeper 中心

- 展示 Controller、注册 Broker 数、Topic 数；
- **Controller 的含义**：它是当前集群里担任「管理者」的 broker 编号（例如 `Controller = 3` 表示 broker-3 是 controller）。
  broker 启动时会抢占 ZooKeeper 的 `/controller` 临时节点，抢到的就成为 controller，节点里存的就是它的 id。
  controller 负责创建 Topic、分配分区的 leader/follower、以及在节点增减/宕机时做再平衡。
  controller 下线后临时节点消失，其余 broker 会在几秒内重新选举（可点「移除」验证：移除 controller 后该数值会变成别的 broker）。
- **加载节点树**：递归查看 `/brokers`、`/topics`、`/controller`、`/zookeeper` 等节点；
- 点选节点后可 **读取**、**写入数据**、**删除**（支持递归删除）；
- 可 **创建节点**（支持持久节点 / 临时节点），例如 `/config/demo`。

### 4.2 Broker 生命周期

- 查看每个 broker 的 UP / DOWN 状态、**端口**与 PID；面板标题显示当前节点总数；
- 通过「Broker ID / 端口」+「新增 Broker」扩容（可指定 id 与端口），或「自动分配一个」；
  行内「移除」随时缩容；
- 对单个 broker 执行 **重启** 或 **强杀**，用于故障演练；
- 强杀 controller 后，约 30 秒（ZooKeeper session 超时）内其他 broker 会重新选举，
  期间其余分区继续对外服务；
- broker 自带**注册自愈**：每 5 秒检查一次 `/brokers/<id>`，若节点丢失（例如强杀后立刻重启
  导致临时节点还没过期、或有人在 ZooKeeper 面板里删掉了它）会自动重新注册，节点不会“隐身”。


### 4.3 生产者

- 创建 Topic（名称 / 分区数 / 副本数），请求会自动路由到 controller；
- 向指定分区发送消息，页面显示写入的 offset。

### 4.4 消费者

| 按钮 | 作用 |
| --- | --- |
| 拉取一次 | 从输入框中的 offset 拉取一次；提示会告诉你本次拉了几条、下次从哪个 offset 继续 |
| **从 0 开始拉取** | 清空列表并把 offset 重置为 0，**分页读到分区末尾**，等价于完整重放该分区 |
| 跳到最新位置 | 先定位到分区末尾并清空列表，之后只拉取新写入的消息（用于"只看新数据"） |
| 开启自动轮询 | 每 1.5 秒从当前 offset 拉一次，读到新消息就追加显示（配合「从 0 开始」可做持续重放） |
| 清空 | 只清空界面展示，不影响服务端数据 |

- offset 是分区内的**绝对序号**：填 0 从头读，填其他值从该位置读；
- **多个客户端各自维护自己的 offset，互不影响** —— 同一个分区可以一个客户端从 0 重放、另一个只追新消息；
- 拉到空数组表示该 offset 已到末尾（不是错误），自动轮询就是靠这个判断「有没有新数据」；
- **不同分区可以有相同的 offset（都从 0 开始）**，列表按「分区 + offset」去重和排序，因此逐个分区拉取时
  每个分区的消息都会展示（标签形如 `P1 #0`）；
- 界面会**按分区记住各自的进度**：切换分区时 offset 输入框自动恢复该分区上次读到的位置。

页面底部另有实时日志与磁盘持久化文件两个面板，每 2.5 秒自动刷新。

> **日志面板说明**：日志只保留可读的关键信息。ZooKeeper 使用 logback，默认（没有配置文件时）会按 DEBUG
> 输出海量日志把面板刷满，因此 `src/main/resources/logback.xml` 已把根级别设为 **WARN**；
> 需要排查 ZooKeeper 内部细节时，把它改成 `DEBUG` 重新打包即可。
> 面板还会自动过滤 java.util.logging 的日期/类名头部、异常堆栈的 `at ...` 帧等噪声。

## 5. 元数据约定（与参考实现一致）

| 内容 | ZooKeeper 路径 / 数据 |
| --- | --- |
| 基础目录 | `/brokers`、`/topics`、`/controller` |
| Broker 注册 | `/brokers/<id>`，数据为 `host:port`（临时节点） |
| Controller 选举 | `/controller`，数据为 broker id（临时节点） |
| Topic 分区分配 | `/topics/<topic>/partitions/<p>`，数据为 `leader;follower1,follower2,...` |

## 6. 命令行使用

```bash
# Broker
java -cp target/simple-kafka-1.0-SNAPSHOT.jar \
  com.simplekafka.broker.SimpleKafkaBroker <brokerId> <host> <port> [zkPort]

# Producer：自动建 Topic 并连续发送 10 条
java -cp target/simple-kafka-1.0-SNAPSHOT.jar \
  com.simplekafka.client.SimpleKafkaProducer localhost 9091 demo-topic

# Consumer：持续消费指定分区
java -cp target/simple-kafka-1.0-SNAPSHOT.jar \
  com.simplekafka.client.SimpleKafkaConsumer localhost 9091 demo-topic 0
```

## 7. HTTP API 一览

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/api/cluster/start` | 启动 ZooKeeper + N 个 broker（`count/basePort/zkPort`，数量不设上限） |
| POST | `/api/cluster/add` | 新增一个节点；`id`/`port` 可省略（自动分配），也可显式指定 |
| POST | `/api/cluster/remove` | 移除并注销一个节点（`id`） |
| POST | `/api/cluster/stop` | 停止控制台托管/接管的全部进程 |
| POST | `/api/process` | 单个进程 `restart/kill/stop`（`name/action`） |
| POST | `/api/topic` | 创建 Topic（`topic/partitions/replicationFactor`） |
| POST | `/api/produce` | 发送消息（`topic/partition/message/port`） |
| POST | `/api/fetch` | 拉取消息（`topic/partition/offset/port`） |
| GET | `/api/state` | 进程 + ZooKeeper 元数据 + 持久化文件 |
| GET | `/api/events` | 实时事件日志 |
| GET | `/api/zk/tree` | 递归读取 znode 树 |
| GET | `/api/zk/read` | 读取节点数据与子节点 |
| POST | `/api/zk/create` | 创建节点（支持临时节点） |
| POST | `/api/zk/set` | 写入节点数据 |
| POST | `/api/zk/delete` | 删除节点（支持递归） |

## 8. 常见问题

- **点击启动显示“集群未启动”**：页面会同时检查控制台进程与 ZooKeeper 注册信息，
  刷新页面即可；旧进程占用端口时会被自动复用。
- **生产/拉取报 `Leader broker not found: N`**：缓存的 leader 指向了已下线节点。
  broker 现在会在发现 leader 不在集群时立即从 ZooKeeper 刷新元数据（保活线程每 5 秒也会刷一次），
  重试一次即可成功。
- **分区分配里出现 `followers=[2,]` 这类已下线节点**：controller 现在只要发现 leader **或任一 follower**
  不在集群中，就会重新指派副本（保留仍存活的副本顺序，再用其它存活 broker 补齐），几秒内自动清理干净。
  具体判定见 README 4.8：存活集合取 ZooKeeper 实时 `/brokers`，且需要「曾见过 + 缺席超过 15 秒」才算确认下线；
  从未见过的副本在启动后 15 秒内不会被清理（避免冷启动时误删还没上线的副本）。
  另外 controller 每 5 秒会周期再平衡一次，所以即使相关 watch 事件丢了也能自愈合。
- **更换 leader 后 offset 从 0 重新开始**：只在副本落后的情况下才会出现。现在 follower 会按 leader 分配的
  offset 落盘（`appendAtOffset`），正常情况下副本之间 offset 严格对齐；但本 demo 没有 ISR / 日志追赶，
  如果某副本在写入期间不在线，它就会落后，此时换成它当 leader 就可能从自己的 offset 继续。
  核对副本是否对齐：比较各 `data/<broker>/<topic>/<partition>/*.log` 的字节数是否完全一致，
  或看控制台磁盘文件面板里的段大小。
  真实 Kafka 用 ISR + leader epoch + 日志截断来保证换主后数据不丢不乱。
- **启动的节点不在页面中显示**：控制台在每次刷新时会自动接管已在运行的 broker；
  若某节点进程仍在但注册节点丢失，broker 会在 5 秒内自动重新注册。
- **创建 Topic 失败（分区数或副本数超出当前 broker 数量?）**：副本数不能大于当前 broker 数量，
  先「增加节点」再创建，或把副本数调小。
- **“failed to locate controller”**：等待 controller 选举完成（约 1~2 秒）后重试；
  dashboard 会清除指向失效 broker 的陈旧 controller 节点。
- **Broker 强杀后短暂不可用**：ZooKeeper 会话需约 30 秒过期，期间该 broker 的
  临时节点仍存在，其他副本可继续服务；过期后自动触发重新选举与再平衡。
- **创建 Topic 长时间无响应 / broker “假死”**：已修复。原因是转发给 controller 的请求可能被转发回自己
  （ZooKeeper 里 controller 是本机、但内存标记未同步），形成自环把工作线程耗尽。
  现在有两道防护：①controller 判定始终以 ZooKeeper 为准（`amController()`）；
  ②识别到“目标是本机”时直接本地创建；③控制面转发改用带
  `SO_TIMEOUT` 的普通 `Socket`（`SocketChannel` 不遵守 `SO_TIMEOUT`，对端不响应会永久阻塞），
  另外工作线程池改为可增长，个别卡住的连接不会再拖垮整个 broker。
- **重启集群后副本变成 `followers=[]`（只剩 leader 一个副本）**：这是修复过的旧 bug ——
  冷启动时最先抢到 controller 的 broker 用还没追平的本地视图做再平衡，把在线副本当死节点删掉了。
  修复后（见 README 4.8 / 5.14）重启不会再发生。**修复前就已经被写坏的 topic 需要手工修一次**：
  在「ZooKeeper 中心」选中 `/topics/<topic>/partitions/<p>`，把数据写回 `leader;follower,`
  （例如 `1;2,`），broker 会在 5 秒内自动刷新并恢复复制。
- **消费者拉取报 `OffsetOutOfRange: offset N is before log start offset M`**：N 以前的数据已被
  **保留策略**删除（默认单个分区最多 8MB / 段最多 1 小时，见 README 4.11）。
  从 M 处重新开始读即可（消费面板点「从最新开始」，或直接把 offset 改成 M）。
  这是有意为之：宁可明确报错，也不会把新数据错位标注成旧 offset 返回。
  想调大保留量：启动 broker 时加 `-Dsimplekafka.retention.bytes=...`（如 64MB），
  段的保留时长用 `-Dsimplekafka.retention.ms=...`，单段大小用 `-Dsimplekafka.segment.bytes=...`。
- **某个副本掉线重启后没有自动补数据**：
  - 掉线**15 秒内**回来：角色保留，pull 复制会在 1~2 秒内自动追平（看 broker 日志的
    `Replica fetch ... appended N record(s)`）。
  - 掉线**超过 15 秒**：controller 已把它当“确认下线”并用其它存活 broker 替换掉，
    它回来时已不是该分区的副本，所以不会拉数据。要恢复需重新分配（把 ZooKeeper 里
    `/topics/<t>/partitions/<p>` 改回包含它的分配，或重建 topic）。
- **一次拉取 1MB 大消息报错**：已修复。客户端原来用固定 64KB 缓冲区读 fetch 响应，大批量会截断
  （`BufferUnderflowException`）。现在按协议长度精确读取，broker 也改为循环写完整响应。
- **想彻底重置**：先点「停止全部」，再删除 `data/` 与 `/tmp/simple-kafka-zk-dashboard`，重新启动即可。

## 9. 持久化、保留策略与复制

每个分区目录（`data/<brokerId>/<topic>/<partition>/`）由一组**日志段** + **稀疏索引**组成，
段文件名就是该段第一条消息的 offset：

```
00000000000000000000.log     每条记录 = [4B 长度][消息体]
00000000000000000000.index   每 ~4KB 数据一条，条目 = [4B 相对 offset][4B 相对 position]
00000000000000000104.log     超过 1MB 后滚动出新段（新段名 = 已分配的下一个 offset）
```

| 机制 | 说明 |
| --- | --- |
| ① 崩溃恢复 | 启动时逐段扫描，遇到不完整/非法记录（长度头被截断、长度非法、消息体不足）立即截断脏尾（`setLength`），保证 offset 不倒退、后续追加不落在半条记录之后 |
| ② 索引重建 | 索引被当作“可再生的加速结构”，每次启动都按日志重建；删掉 `*.index` 也能自愈，不影响数据 |
| ③ 跨段读取 | 逐段循环读取，段内从稀疏索引定位后顺序扫描，读到段末尾自动切到下一段——跨越 1MB 边界也能连续拉取 |
| ④ 稀疏索引 | 从“每条消息 16B”改为“每 ~4KB 一条 8B”，索引体积约为日志的 0.1%~0.2%；读取时先二分到不大于目标的条目再顺序扫描 |
| ⑤ 段轮转 | 写入前判断本段是否放得下这条消息，放不下且本段非空时滚动新段，段不会超上限也不会产生空段；索引始终写当前活跃段 |
| **保留策略** | 按大小（默认 8MB/分区 `simplekafka.retention.bytes`）或时间（默认 1 小时/段 `simplekafka.retention.ms`）**整段删除**旧段；删完 `logStartOffset` 前移，消费者读被删的 offset 会收到 `OffsetOutOfRange` 而不会被错位糊弄 |
| **副本复制** | follower **主动拉取**（`REPLICA_FETCH` + 长轮询 + 长连接）：掉线重启后 1~2 秒自动追平；leader 起点因保留策略前移时，follower 会 `truncateBefore` 对齐自己的起点 |

**验证方式**（都可以在页面上实操）：

1. 往一个分区连续发送 120 条 10KB 消息 → 磁盘文件面板出现两个 `*.log`，索引只有几百字节。
2. 消费者从 `offset=0` 开始持续拉取 → 每次 1MB 以内的消息连续返回，offset 严格递增（跨段不错位）。
3. 停止全部进程，手工往最后一个 `*.log` 追加几个垃圾字节，再启动 → 文件被自动截断回原长度，
   消息条数与 offset 保持不变，新消息从原 offset 继续。

> 仍未实现（后续阶段）：保留策略与日志清理、ISR/高水位/leader epoch、索引 mmap 与批量刷盘、
> 消费者位点持久化。目前每条消息都会 `force(true)`，写入吞吐以“可讲解”为优先。
